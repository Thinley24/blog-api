<h1>Articles</h1>

<i>Our blog has <b><%= Article.public_count %></b> articles and counting!</i>

<ul>
    <% @articles.each do |article| %>
        <% unless article.archived? %>
            <li>
                <%= link_to article.title, article %>
            </li>
        <% end %>
    <%end%>
</ul>

<%= link_to "New Article", new_article_path %>